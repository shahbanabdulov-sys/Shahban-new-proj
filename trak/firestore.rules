rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Каждый пользователь может читать/писать только свой документ в productivityData
    match /productivityData/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}


